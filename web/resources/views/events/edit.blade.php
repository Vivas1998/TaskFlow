<x-layouts.app title="Editar evento" :projects="$projects" :active-project="$project">
    <div class="page-narrow"><a class="back-link" href="{{ route('calendar.index', $project) }}">← Volver al calendario</a><div class="dashboard__heading"><div><p class="dashboard__eyebrow">{{ $project->name }}</p><h1 class="dashboard__title">Editar evento</h1></div></div><section class="panel form-panel"><x-event-form :action="route('events.update', [$project, $event])" method="PUT" :project="$project" :event="$event" :colors="$colors" /></section><form class="archive-action" action="{{ route('events.destroy', [$project, $event]) }}" method="post">@csrf @method('DELETE')<button class="button button--danger" type="submit">Enviar a la papelera</button><p>Podrás restaurarlo después.</p></form></div>
</x-layouts.app>
